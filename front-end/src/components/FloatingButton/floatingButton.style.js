import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  buttonWrapper: {
    height: 70,
    width: 70,
    position: 'absolute',
    bottom: 40,
    right: 20,
  }
});

export default styles;
