const ImageDriverFactory = async ({
  wrapperComponent,
  testID
}) => {
  const text = await wrapperComponent.queryByTestId(testID);
  return {
    //todo more research on this component
    exists: () => !!text,
    getRootElement: () => text // getImageContent: () => text.props.value

  };
};

export default ImageDriverFactory;